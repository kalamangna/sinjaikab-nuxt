{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/sync-sitemap-data.mjs && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.2.0",
    "@nuxtjs/tailwindcss": "^6.8.0",
    "@vercel/analytics": "^2.0.1",
    "nuxt": "^3.7.3"
  }
}
